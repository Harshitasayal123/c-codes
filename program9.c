// wap to find the largest no among two using ternary operator.
#include<stdio.h>
void main ()
{
    int x, y;
    printf("Enter the value of x and y :   ");
    scanf("%d%d",&x,&y);
    (x>y)? printf("x is greater than y") : printf("y is greater than x");

    
}